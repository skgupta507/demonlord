/* eslint-disable prettier/prettier */
// Landing page layout — completely bare, no sidebar, no header, no wrappers
export default function LandingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
