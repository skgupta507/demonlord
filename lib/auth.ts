/* eslint-disable prettier/prettier */
// Auth is handled by Firebase. See lib/firebase/auth-context.tsx
// This stub prevents import errors from stale references in the codebase.
export const auth = null;
